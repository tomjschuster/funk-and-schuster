<h2>Edit Work for <%= @work.artist.first_name <> " " <> @work.artist.last_name %></h2>

<% action = artist_work_path(@conn, :update, @work.artist, @work) %>

<%= render "form-for-artist.html", Map.merge(assigns, %{action: action}) %>

<span><%= link "Back", to: artist_work_path(@conn, :index, @work.artist) %></span>
