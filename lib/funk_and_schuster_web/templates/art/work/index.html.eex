<h2>Listing Works</h2>

<span><%= link "New Work", to: work_path(@conn, :new) %></span>
<span><%= link "Back", to: art_path(@conn, :index) %></span>

<%= render "_list.html", conn: @conn, works: @works %>
