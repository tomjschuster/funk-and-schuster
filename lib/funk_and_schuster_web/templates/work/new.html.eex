<h2>New Work</h2>

<%= render "form.html", Map.merge(assigns, %{action: artist_work_path(@conn, :create, @artist), method: "post"}) %>

<span><%= link "Back", to: artist_work_path(@conn, :index, @artist) %></span>
